
function sumas(a: number, b: number): number {
  return a + b;
}

console.log(sumas(5, 3));

// tsc Suma.ts para compilar el archivo TypeScript a JavaScript
// node Suma.js para ejecutar el archivo JavaScript generado
